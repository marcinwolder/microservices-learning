apiVersion: apps/v1
kind: Deployment
metadata:
  name: tickets-mysql-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tickets-mysql
  template:
    metadata:
      labels:
        app: tickets-mysql
    spec:
      containers:
        - name: tickets-mysql
          image: mysql